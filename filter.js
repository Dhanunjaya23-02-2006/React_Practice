//Creates a new array with elements that pass a condition

//Use case:When you need to filter elements based on a criterion

let numbers=[1,2,3,4,5,6];

let even=numbers.filter((num) => num%2 == 0);

console.log(even);
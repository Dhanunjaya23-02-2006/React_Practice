//creates a new array by applying a function to each element

//Use case:When you need to transform an array without modifying the original array

let numbers=[1,2,3,4];
let doubled=numbers.map((numbers) => numbers**3);

console.log(doubled);